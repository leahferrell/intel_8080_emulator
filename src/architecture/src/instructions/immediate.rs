use crate::opcodes::OpCode;
use crate::state::State;
use crate::instruction::Instruction;

pub fn load_register_pair(state: &State, instruction: &Instruction) -> bool {
    println!("loading register pair");
    false
}